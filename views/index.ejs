<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="https://cdn.jsdelivr.net/npm/toastify-js/src/toastify.min.css">
       <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form max-w-xl mx-auto p-6 bg-black text-white rounded-md shadow-md">
            <form action="/create" method="post" class="space-y-4">
                <div>
                    <label for="title" class="block text-sm font-medium">Title</label>
                    <input name="title" type="text" id="title" name="title" placeholder="Enter title" class="mt-1 p-2 w-full   bg-gray-800 border rounded-md text-white">
                </div>
                
                <div>
                    <label for="description" class="block text-sm font-medium">Description</label>
                    <textarea name="description" id="description" name="description" cols="30" rows="10" placeholder="Write here" class="mt-1 resize-none p-2 w-full bg-gray-800 border rounded-md text-white"></textarea>
                </div>
                
                <div>
                    <button type="submit" class="w-full bg-blue-500 text-white p-2 rounded-md hover:bg-blue-600">Create Task</button>
                </div>
            </form>
        </div>
        

        <div class="tasks p-10 flex space-x-6 overflow-x-auto  text-white">
          <% if(files.length > 0 ){%>
            <% files.forEach(function(val){%>
                <div class="task flex-shrink-0 bg-gray-800 rounded-lg shadow-md p-6">
                    <p class="text-xl min-w-full font-medium"><%= val %></p>
                    <a href="/file/<%= val %>" class="text-blue-400 hover:text-blue-300 mt-2 inline-block">read more</a>
                    <a href="/edit/<%= val %>" class="text-gray-400 hover:text-blue-300 mt-2 ml-8 inline-block">edit file name</a>
                <div >
                    <a href="/delete/<%= val %>" class="bg-red-500 hover:bg-red-600 text-white py-2 px-4 rounded-lg mt-4 inline-block" onclick= confirmDelete() >
                       <h6  >Delete</h6>
                    </a>
                </div>
                   
                </div>
                
            <%}) %>

          <% } else{ %>

            <div class="task flex-shrink-0 bg-gray-800 rounded-lg shadow-md p-6">
                <p class="text-xl font-medium">No tasks here ಥ_ಥ</p>
                <a href="" class="text-blue-400 hover:text-blue-300 mt-2 inline-block">add some tasks</a>
            </div>
        
         <% }

            
            %>

            
        
            
           
            
            <!-- Add more tasks as needed -->
        
        </div>
        




    </div>
</body>
<script type="text/javascript" src="https://cdn.jsdelivr.net/npm/toastify-js"></script>
<script>
   
function confirmDelete() {
        if (confirm("Are you sure you want to delete?")) {
            // Show Toastify notification
            Toastify({
                text: "Deleted successfully",
                backgroundColor: "linear-gradient(to right, #ff4b2b, #ff416c)",
                className: "info",
            }).showToast();
            
            return true;
        } else {
            return false;
        }
    }


</script>
</html>